---
layout: null
---
{
    "version": "https://jsonfeed.org/version/1",
    "title": "{{ site.title | xml_escape }}",
    "home_page_url": "{{ "/" | absolute_url }}",
    "feed_url": "{{ site.social-rss | absolute_url }}",
    "description": "{{ site.description | xml_escape }}",
    "icon": "{{ "/apple-touch-icon.png" | absolute_url }}",
    "favicon": "{{ "/favicon.ico" | absolute_url }}",
    "expired": false,
    "author" : {
        "url" : "{{ site.author_url }}",
        "name" : "{{ site.author }}"
},

"items": [
    {% for post in site.posts limit:20 %}
        {
            "title": {{ post.title | jsonify }},
            "id": "{{ post.url | absolute_url | sha1 }}",
            "url": "{{ post.url | absolute_url }}",
            "content_text": {{ post.content | strip_html | strip_newlines | jsonify }},
            "content_html": {{ post.content | strip_newlines | jsonify }},
            {% if post.tn %}"image": "{{ site.url}}/assets/img/index/{{ post.tn }}",{% endif %}
            {% if post.tags.size > 1 %}"tags": {{ post.tags | jsonify }},{% endif %}
            "date_published": "{{ post.date | date_to_xmlschema }}",
            "date_modified": "{{ post.date | date_to_xmlschema }}"
        }{% if forloop.last == false %},{% endif %}
    {% endfor %}
    ]
}